@extends('layouts.app')

@section('content')
<div class="max-w-7xl mx-auto px-4 py-6"><x-cashless.shell title="{{ $session->session_number }}" subtitle="{{ $session->merchant?->name }}"><div class="grid gap-4 md:grid-cols-3"><div class="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">Kasir: {{ $session->cashier?->name }}<br>Status: {{ $session->status }}</div><div class="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">Sales: Rp {{ number_format($session->total_sales,0,',','.') }}<br>Refund: Rp {{ number_format($session->total_refunds,0,',','.') }}</div><div class="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900">@if($session->status === 'open')<form method="POST" action="{{ route('cashless.pos-sessions.close', $session) }}">@csrf @method('PATCH')<input name="closing_note" placeholder="Catatan tutup" class="mb-2 w-full rounded-lg dark:border-slate-700 dark:bg-slate-950"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-black text-white dark:bg-lime-400 dark:text-slate-950">Tutup Session</button></form>@endif</div></div></x-cashless.shell></div>
@endsection
