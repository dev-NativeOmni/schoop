@extends('layouts.app')

@section('content')
<div class="max-w-7xl mx-auto px-4 py-6"><x-cashless.shell title="Refund / Void Transaksi"><form method="POST" action="{{ route('cashless.refunds.store') }}" class="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900">@csrf<label class="block text-sm font-bold dark:text-slate-200">Transaksi <select name="cashless_sale_id" class="mt-1 w-full rounded-lg dark:border-slate-700 dark:bg-slate-950">@foreach($sales as $sale)<option value="{{ $sale->id }}">{{ $sale->receipt_number }} · {{ $sale->student?->full_name }} · sisa Rp {{ number_format($sale->total_amount - $sale->refunded_amount,0,',','.') }}</option>@endforeach</select></label><label class="mt-4 block text-sm font-bold dark:text-slate-200">Nominal <input name="amount" type="number" min="1" class="mt-1 w-full rounded-lg dark:border-slate-700 dark:bg-slate-950" required></label><label class="mt-4 block text-sm font-bold dark:text-slate-200">Alasan <textarea name="reason" class="mt-1 w-full rounded-lg dark:border-slate-700 dark:bg-slate-950" required></textarea></label><button class="mt-5 rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Post Refund</button></form></x-cashless.shell></div>
@endsection
