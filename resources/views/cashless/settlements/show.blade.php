@extends('layouts.app')

@section('content')
<div class="max-w-7xl mx-auto px-4 py-6"><x-cashless.shell title="{{ $settlement->settlement_number }}" subtitle="{{ $settlement->merchant?->name }}"><div class="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">Periode: {{ $settlement->period_start->format('d M Y') }} - {{ $settlement->period_end->format('d M Y') }}<br>Sales: Rp {{ number_format($settlement->total_sales,0,',','.') }}<br>Refund: Rp {{ number_format($settlement->total_refunds,0,',','.') }}<br>Net: Rp {{ number_format($settlement->net_sales,0,',','.') }}<br>Status: {{ $settlement->status }}</div><div class="flex gap-2">@if($settlement->status !== 'approved')<form method="POST" action="{{ route('cashless.settlements.approve', $settlement) }}">@csrf @method('PATCH')<button class="rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Approve</button></form>@endif<form method="POST" action="{{ route('cashless.settlements.void', $settlement) }}">@csrf @method('PATCH')<button class="rounded-lg bg-red-500 px-4 py-2 text-sm font-black text-white">Void</button></form></div></x-cashless.shell></div>
@endsection
