@extends('layouts.app')

@section('content')
<div class="max-w-7xl mx-auto px-4 py-6"><x-cashless.shell title="Settlement Merchant"><form method="POST" action="{{ route('cashless.settlements.store') }}" class="grid gap-3 rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 md:grid-cols-4">@csrf<select name="cashless_merchant_id" class="rounded-lg dark:border-slate-700 dark:bg-slate-950">@foreach($merchants as $merchant)<option value="{{ $merchant->id }}">{{ $merchant->name }}</option>@endforeach</select><input type="date" name="period_start" value="{{ now()->toDateString() }}" class="rounded-lg dark:border-slate-700 dark:bg-slate-950"><input type="date" name="period_end" value="{{ now()->toDateString() }}" class="rounded-lg dark:border-slate-700 dark:bg-slate-950"><button class="rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Buat</button></form><div class="overflow-x-auto rounded-xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><table class="min-w-full text-sm"><tbody>@foreach($settlements as $settlement)<tr class="border-b dark:border-slate-800 dark:text-slate-200"><td class="p-3"><a class="font-bold" href="{{ route('cashless.settlements.show', $settlement) }}">{{ $settlement->settlement_number }}</a></td><td>{{ $settlement->merchant?->name }}</td><td>Rp {{ number_format($settlement->net_sales,0,',','.') }}</td><td>{{ $settlement->status }}</td></tr>@endforeach</tbody></table></div>{{ $settlements->links() }}</x-cashless.shell></div>
@endsection
