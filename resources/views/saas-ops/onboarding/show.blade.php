@extends('layouts.app')
@section('content')<div class="max-w-7xl mx-auto px-4 py-6"><x-saas-ops.shell title="Onboarding Checklist" subtitle="{{ $project->title }}"><div class="space-y-2">@foreach($project->records as $record)<div class="rounded-xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200"><div class="flex items-center justify-between gap-3"><span>{{ $record->item?->title }} - {{ $record->status }}</span>@if($record->status !== 'done')<form method="POST" action="{{ route('saas-ops.onboarding.complete',[$project,$record]) }}">@csrf<button class="rounded-lg bg-lime-500 px-3 py-2 text-xs font-black text-white">Complete</button></form>@endif</div></div>@endforeach</div></x-saas-ops.shell></div>@endsection
