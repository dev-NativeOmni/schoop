@extends('layouts.app')
@section('content')<div class="max-w-7xl mx-auto px-4 py-6"><x-saas-ops.shell title="{{ $ticket->ticket_number }}" subtitle="{{ $ticket->subject }} - {{ $ticket->status }}"><div class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">{{ $ticket->description }}<br>SLA breached: {{ $ticket->sla_breached ? 'yes' : 'no' }}</div><form method="POST" action="{{ route('saas-ops.support-tickets.messages.store',$ticket) }}" class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900">@csrf<select name="visibility" class="rounded-lg dark:bg-slate-950"><option value="public">public</option><option value="internal">internal</option></select><textarea name="message" class="mt-3 w-full rounded-lg dark:bg-slate-950"></textarea><button class="mt-3 rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Add Message</button></form><div class="flex flex-wrap gap-2"><form method="POST" action="{{ route('saas-ops.support-tickets.assign',$ticket) }}">@csrf<select name="user_id" class="rounded-lg dark:bg-slate-950">@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-3 py-2 text-xs font-black text-white">Assign</button></form><form method="POST" action="{{ route('saas-ops.support-tickets.resolve',$ticket) }}">@csrf<button class="rounded-lg bg-lime-500 px-3 py-2 text-xs font-black text-white">Resolve</button></form><form method="POST" action="{{ route('saas-ops.support-tickets.close',$ticket) }}">@csrf<button class="rounded-lg bg-red-500 px-3 py-2 text-xs font-black text-white">Close</button></form></div><div class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-black dark:text-white">Messages</h2>@foreach($ticket->messages as $message)<p class="mt-2 text-sm dark:text-slate-200">[{{ $message->visibility }}] {{ $message->message }}</p>@endforeach</div></x-saas-ops.shell></div>@endsection
