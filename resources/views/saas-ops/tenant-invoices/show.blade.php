@extends('layouts.app')
@section('content')<div class="max-w-7xl mx-auto px-4 py-6"><x-saas-ops.shell title="{{ $invoice->invoice_number }}" subtitle="{{ $invoice->school?->name }} - {{ $invoice->status }}"><div class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">Total: Rp {{ number_format($invoice->total_amount,0,',','.') }}<br>Paid: Rp {{ number_format($invoice->paid_amount,0,',','.') }}<br>Balance: Rp {{ number_format($invoice->balance_amount,0,',','.') }}</div><div class="flex flex-wrap gap-2"><form method="POST" action="{{ route('saas-ops.tenant-invoices.issue',$invoice) }}">@csrf<button class="rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Issue</button></form><form method="POST" action="{{ route('saas-ops.tenant-invoices.payments.store',$invoice) }}">@csrf<input type="number" name="amount" placeholder="amount" class="rounded-lg dark:bg-slate-950"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-black text-white">Post Payment</button></form><form method="POST" action="{{ route('saas-ops.tenant-invoices.void',$invoice) }}">@csrf<input name="reason" placeholder="reason" class="rounded-lg dark:bg-slate-950"><button class="rounded-lg bg-red-500 px-4 py-2 text-sm font-black text-white">Void</button></form></div><div class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 class="font-black dark:text-white">Items</h2>@foreach($invoice->items as $item)<p class="text-sm dark:text-slate-200">{{ $item->description }} - Rp {{ number_format($item->amount,0,',','.') }}</p>@endforeach</div></x-saas-ops.shell></div>@endsection
