@extends('layouts.app')
@section('content')<div class="max-w-7xl mx-auto px-4 py-6"><x-saas-ops.shell title="{{ $subscription->school?->name }}" subtitle="{{ $subscription->plan?->name }} - {{ $subscription->status }}"><div class="flex flex-wrap gap-2"><form method="POST" action="{{ route('saas-ops.school-subscriptions.activate',$subscription) }}">@csrf<button class="rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Activate</button></form><form method="POST" action="{{ route('saas-ops.school-subscriptions.suspend',$subscription) }}">@csrf<input name="reason" placeholder="Alasan suspend" class="rounded-lg dark:bg-slate-950"><button class="rounded-lg bg-amber-500 px-4 py-2 text-sm font-black text-white">Suspend</button></form><form method="POST" action="{{ route('saas-ops.school-subscriptions.cancel',$subscription) }}">@csrf<input name="reason" placeholder="Alasan cancel" class="rounded-lg dark:bg-slate-950"><button class="rounded-lg bg-red-500 px-4 py-2 text-sm font-black text-white">Cancel</button></form></div><div class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900 dark:text-slate-200">Billing: {{ $subscription->billing_cycle }}<br>Period: {{ $subscription->current_period_start?->format('d M Y') }} - {{ $subscription->current_period_end?->format('d M Y') }}<br>Trial ends: {{ $subscription->trial_ends_at?->format('d M Y') }}</div></x-saas-ops.shell></div>@endsection
