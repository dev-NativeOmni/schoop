<form method="POST" action="{{ $policy ? route('saas-ops.sla-policies.update',$policy) : route('saas-ops.sla-policies.store') }}" class="rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900">@csrf @if($policy) @method('PUT') @endif<div class="grid gap-4 md:grid-cols-3"><select name="priority" class="rounded-lg dark:bg-slate-950">@foreach(['critical','high','medium','low'] as $priority)<option value="{{ $priority }}" @selected(old('priority',$policy?->priority)===$priority)>{{ $priority }}</option>@endforeach</select><input type="number" name="first_response_minutes" value="{{ old('first_response_minutes',$policy?->first_response_minutes) }}" placeholder="first response minutes" class="rounded-lg dark:bg-slate-950"><input type="number" name="resolution_minutes" value="{{ old('resolution_minutes',$policy?->resolution_minutes) }}" placeholder="resolution minutes" class="rounded-lg dark:bg-slate-950"></div><label class="mt-3 flex gap-2 text-sm dark:text-slate-200"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$policy?->is_active ?? true))> Active</label><textarea name="notes" class="mt-3 w-full rounded-lg dark:bg-slate-950">{{ old('notes',$policy?->notes) }}</textarea><button class="mt-3 rounded-lg bg-lime-500 px-4 py-2 text-sm font-black text-white">Simpan</button></form>
